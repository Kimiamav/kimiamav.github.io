![[Pasted image 20260115110710.png]]

# 1)QUESTIONS/Définitions POUR LE PROJET JAVAFX (a retenir par cœur)

1. Qu'est-ce qu'une variable ? 
	une variable est un outil contenant une donnée, par exemple un mot de passe ou un chiffre, qu'on doit déclarer pour qu'elle existe. 
 a. Les différents types de variables. 
		 int = que des nombres entiers ;
		 double ou float = que des nombres décimal ;
		 String = texte ;
		 Boolean = booléens
		 long des nombres entiers plus grand 
		 
 b. Les différents opérateurs arithmétiques.
	- + addition ;
	-  - soustraction ;
	- * multiplication ;
	- / division ;
 c. Constantes
	- il est recommandé d'utiliser des constantes dans la mesure du possible. 
	- Pour déclarer une constantes en java, il faut utiliser le mot clé *final*

 [^1]: - Une **déclaration** de **variable** est composée de trois éléments : type, **nom**, et **valeur**.
	    
	- Les **valeurs** des variables peuvent être **modifiées**.
	    
	- Les variables à valeurs constantes sont appelées **constantes**.
	    
	- Le nom des variables doit respecter les **conventions de dénomination** courantes

2. C'est quoi exactement une fonction ? 
	Une classe, c'est un ensemble : 
	 - de variables, nommées attributs ; 
	 - et de comportements, nommés méthodes. 
	  Les classes représentent les patrons de construction des objets de votre programme. 
	Une fonction peut être considérée comme un bloc de code avec un nom qui exécute un service. elle s'appelle une **méthode**. 
		==String== est une classe car il commence par une Majuscule mais aussi il est définit comme état et comportement : 
		- Son état est la chaine de caractères que vous stockez 
		- Son comportement est l'ensemble des méthodes que la classe *String* définit
		- 

   [^2]- Les programmes Java sont structurés en **packages** et en **classes**.
	    
	- Aucun code n'est écrit en dehors d'une classe, ce qui signifie que **toutes les fonctions sont des méthodes** en Java.
	    
	- Les packages sont mappés dans des **dossiers** et les classes dans des **fichiers**.
	    
	- La commande `**javac**` convertit le code Java en **Bytecode**.
	    
	- La commande `**java**` exécute le programme actuel en exécutant la fonction   `main`  dans la classe fournie.
	    
	- Il existe deux types de classes :
	    
	
	1. Les **classes modèles** qui sont utilisées comme modèles pour l'instanciation des objets.
	    
	2. Les **classes utilitaires** qui contiennent des méthodes statiques qui peuvent être appelées directement sur la classe.
	    
	
	-

